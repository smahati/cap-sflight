{
  "name": "sflight-mtx",
  "dependencies": {
    "@sap/cds": "^6",
    "@sap/cds-mtxs": "^1",
    "@sap/xssec": "^3",
    "express": "^4",
    "hdb": "^0",
    "passport": "^0.6.0"
  },
  "devDependencies": {
    "sqlite3": "^5"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "npm ci && npx -p @sap/cds-dk@^6 cds build ../.. --for mtx-sidecar --production && cd gen && npm ci"
  },
  "cds": {
    "profile": "mtx-sidecar",
    "requires": {
      "auth": {
        "kind": "dummy-auth"
      }
    }
  }
}
